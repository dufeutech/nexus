# nexus-owned-workspace-tenancy

Own tenancy/authorization in nexus (Account owns Workspace; typed staff|customer memberships resolved live by the identity plane); ZITADEL is authentication-only. Rename routing tenant_id -> workspace_id; redesign the trusted header contract.
