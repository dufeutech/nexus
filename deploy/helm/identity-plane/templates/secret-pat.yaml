{{- if not .Values.zitadel.patSecret.existingSecret -}}
# Created only when zitadel.patSecret.existingSecret is empty. PREFER an existing
# Secret (or an ExternalSecret / SealedSecret) so the PAT never lives in values
# or the rendered release manifest. This admin PAT is a high-privilege credential
# (it can register webhooks and list every user) — treat it accordingly.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "identity-plane.patSecretName" . }}
  labels:
    {{- include "identity-plane.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ .Values.zitadel.patSecret.key }}: {{ required "Set zitadel.patSecret.value or zitadel.patSecret.existingSecret" .Values.zitadel.patSecret.value | quote }}
{{- end }}
