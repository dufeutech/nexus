{{- if eq .Values.monitoring.delivery "operator" -}}
# operator-form delivery (monitoring.delivery=operator): nexus-authored application-SLO
# alert rules as a PrometheusRule CR, consumed by the Prometheus Operator OR the
# VictoriaMetrics operator/vmalert. The rule bodies come from the shared
# "identity-plane.appSloGroups" helper, so the files-form ConfigMap
# (monitoring-rules-files.yaml) ships byte-identical content. Requires that operator's CRD.
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "identity-plane.fullname" . }}-app-slo
  labels:
    {{- include "identity-plane.labels" . | nindent 4 }}
    {{- with .Values.monitoring.operator.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- include "identity-plane.appSloGroups" . | nindent 2 }}
{{- end }}
