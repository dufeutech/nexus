
---
# Code generated by Sloth (v0.16.0): https://github.com/slok/sloth.
# DO NOT EDIT.

groups:
- name: sloth-slo-sli-recordings-identity-sidecar-availability
  rules:
  - record: slo:sli_error:ratio_rate5m
    expr: |
      (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total{result=~"not_ready|unavailable_closed"}[5m])))
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[5m]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[5m])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_window: 5m
  - record: slo:sli_error:ratio_rate30m
    expr: |
      (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total{result=~"not_ready|unavailable_closed"}[30m])))
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[30m]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[30m])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_window: 30m
  - record: slo:sli_error:ratio_rate1h
    expr: |
      (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total{result=~"not_ready|unavailable_closed"}[1h])))
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[1h]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[1h])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_window: 1h
  - record: slo:sli_error:ratio_rate2h
    expr: |
      (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total{result=~"not_ready|unavailable_closed"}[2h])))
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[2h]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[2h])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_window: 2h
  - record: slo:sli_error:ratio_rate6h
    expr: |
      (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total{result=~"not_ready|unavailable_closed"}[6h])))
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[6h]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[6h])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_window: 6h
  - record: slo:sli_error:ratio_rate1d
    expr: |
      (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total{result=~"not_ready|unavailable_closed"}[1d])))
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[1d]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[1d])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_window: 1d
  - record: slo:sli_error:ratio_rate3d
    expr: |
      (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total{result=~"not_ready|unavailable_closed"}[3d])))
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[3d]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_requests_total[3d])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_window: 3d
  - record: slo:sli_error:ratio_rate30d
    expr: |
      sum_over_time(slo:sli_error:ratio_rate5m{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"}[30d])
      / ignoring (sloth_window)
      count_over_time(slo:sli_error:ratio_rate5m{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"}[30d])
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_window: 30d
- name: sloth-slo-meta-recordings-identity-sidecar-availability
  rules:
  - record: slo:objective:ratio
    expr: vector(0.9990000000000001)
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
  - record: slo:error_budget:ratio
    expr: vector(1-0.9990000000000001)
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
  - record: slo:time_period:days
    expr: vector(30)
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
  - record: slo:current_burn_rate:ratio
    expr: |
      slo:sli_error:ratio_rate5m{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"}
      / on(sloth_id, sloth_slo, sloth_service) group_left
      slo:error_budget:ratio{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"}
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
  - record: slo:period_burn_rate:ratio
    expr: |
      slo:sli_error:ratio_rate30d{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"}
      / on(sloth_id, sloth_slo, sloth_service) group_left
      slo:error_budget:ratio{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"}
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
  - record: slo:period_error_budget_remaining:ratio
    expr: 1 - slo:period_burn_rate:ratio{sloth_id="identity-sidecar-availability",
      sloth_service="identity-sidecar", sloth_slo="availability"}
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_service: identity-sidecar
      sloth_slo: availability
  - record: sloth_slo_info
    expr: vector(1)
    labels:
      plane: identity
      sloth_id: identity-sidecar-availability
      sloth_mode: cli-gen-prom
      sloth_objective: "99.9"
      sloth_service: identity-sidecar
      sloth_slo: availability
      sloth_spec: prometheus/v1
      sloth_version: v0.16.0
- name: sloth-slo-alerts-identity-sidecar-availability
  rules:
  - alert: IdentitySidecarAvailability
    expr: |
      (
          max(slo:sli_error:ratio_rate5m{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"} > (14.4 * 0.0009999999999999432)) without (sloth_window)
          and
          max(slo:sli_error:ratio_rate1h{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"} > (14.4 * 0.0009999999999999432)) without (sloth_window)
      )
      or
      (
          max(slo:sli_error:ratio_rate30m{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"} > (6 * 0.0009999999999999432)) without (sloth_window)
          and
          max(slo:sli_error:ratio_rate6h{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"} > (6 * 0.0009999999999999432)) without (sloth_window)
      )
    labels:
      plane: identity
      severity: page
      sloth_severity: page
    annotations:
      summary: Identity-sidecar availability error budget is burning
      title: (page) {{$labels.sloth_service}} {{$labels.sloth_slo}} SLO error budget
        burn rate is too fast.
  - alert: IdentitySidecarAvailability
    expr: |
      (
          max(slo:sli_error:ratio_rate2h{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"} > (3 * 0.0009999999999999432)) without (sloth_window)
          and
          max(slo:sli_error:ratio_rate1d{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"} > (3 * 0.0009999999999999432)) without (sloth_window)
      )
      or
      (
          max(slo:sli_error:ratio_rate6h{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"} > (1 * 0.0009999999999999432)) without (sloth_window)
          and
          max(slo:sli_error:ratio_rate3d{sloth_id="identity-sidecar-availability", sloth_service="identity-sidecar", sloth_slo="availability"} > (1 * 0.0009999999999999432)) without (sloth_window)
      )
    labels:
      plane: identity
      severity: ticket
      sloth_severity: ticket
    annotations:
      summary: Identity-sidecar availability error budget is burning
      title: (ticket) {{$labels.sloth_service}} {{$labels.sloth_slo}} SLO error budget
        burn rate is too fast.
- name: sloth-slo-sli-recordings-identity-sidecar-latency
  rules:
  - record: slo:sli_error:ratio_rate5m
    expr: |
      ((
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[5m]))
        -
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_bucket{result!~"not_ready|unavailable_closed",le="0.1"}[5m]))
      )
      )
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[5m]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[5m])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_window: 5m
  - record: slo:sli_error:ratio_rate30m
    expr: |
      ((
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[30m]))
        -
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_bucket{result!~"not_ready|unavailable_closed",le="0.1"}[30m]))
      )
      )
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[30m]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[30m])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_window: 30m
  - record: slo:sli_error:ratio_rate1h
    expr: |
      ((
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[1h]))
        -
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_bucket{result!~"not_ready|unavailable_closed",le="0.1"}[1h]))
      )
      )
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[1h]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[1h])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_window: 1h
  - record: slo:sli_error:ratio_rate2h
    expr: |
      ((
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[2h]))
        -
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_bucket{result!~"not_ready|unavailable_closed",le="0.1"}[2h]))
      )
      )
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[2h]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[2h])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_window: 2h
  - record: slo:sli_error:ratio_rate6h
    expr: |
      ((
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[6h]))
        -
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_bucket{result!~"not_ready|unavailable_closed",le="0.1"}[6h]))
      )
      )
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[6h]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[6h])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_window: 6h
  - record: slo:sli_error:ratio_rate1d
    expr: |
      ((
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[1d]))
        -
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_bucket{result!~"not_ready|unavailable_closed",le="0.1"}[1d]))
      )
      )
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[1d]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[1d])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_window: 1d
  - record: slo:sli_error:ratio_rate3d
    expr: |
      ((
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[3d]))
        -
        sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_bucket{result!~"not_ready|unavailable_closed",le="0.1"}[3d]))
      )
      )
      /
      ((sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[3d]))) and (sum by (deployment_environment_name) (rate(sidecar_ext_proc_duration_seconds_count{result!~"not_ready|unavailable_closed"}[3d])) > 0.2))
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_window: 3d
  - record: slo:sli_error:ratio_rate30d
    expr: |
      sum_over_time(slo:sli_error:ratio_rate5m{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"}[30d])
      / ignoring (sloth_window)
      count_over_time(slo:sli_error:ratio_rate5m{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"}[30d])
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_window: 30d
- name: sloth-slo-meta-recordings-identity-sidecar-latency
  rules:
  - record: slo:objective:ratio
    expr: vector(0.99)
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
  - record: slo:error_budget:ratio
    expr: vector(1-0.99)
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
  - record: slo:time_period:days
    expr: vector(30)
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
  - record: slo:current_burn_rate:ratio
    expr: |
      slo:sli_error:ratio_rate5m{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"}
      / on(sloth_id, sloth_slo, sloth_service) group_left
      slo:error_budget:ratio{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"}
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
  - record: slo:period_burn_rate:ratio
    expr: |
      slo:sli_error:ratio_rate30d{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"}
      / on(sloth_id, sloth_slo, sloth_service) group_left
      slo:error_budget:ratio{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"}
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
  - record: slo:period_error_budget_remaining:ratio
    expr: 1 - slo:period_burn_rate:ratio{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar",
      sloth_slo="latency"}
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_service: identity-sidecar
      sloth_slo: latency
  - record: sloth_slo_info
    expr: vector(1)
    labels:
      plane: identity
      sloth_id: identity-sidecar-latency
      sloth_mode: cli-gen-prom
      sloth_objective: "99"
      sloth_service: identity-sidecar
      sloth_slo: latency
      sloth_spec: prometheus/v1
      sloth_version: v0.16.0
- name: sloth-slo-alerts-identity-sidecar-latency
  rules:
  - alert: IdentitySidecarLatency
    expr: |
      (
          max(slo:sli_error:ratio_rate5m{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"} > (14.4 * 0.01)) without (sloth_window)
          and
          max(slo:sli_error:ratio_rate1h{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"} > (14.4 * 0.01)) without (sloth_window)
      )
      or
      (
          max(slo:sli_error:ratio_rate30m{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"} > (6 * 0.01)) without (sloth_window)
          and
          max(slo:sli_error:ratio_rate6h{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"} > (6 * 0.01)) without (sloth_window)
      )
    labels:
      plane: identity
      severity: page
      sloth_severity: page
    annotations:
      summary: Identity-sidecar latency error budget is burning
      title: (page) {{$labels.sloth_service}} {{$labels.sloth_slo}} SLO error budget
        burn rate is too fast.
  - alert: IdentitySidecarLatency
    expr: |
      (
          max(slo:sli_error:ratio_rate2h{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"} > (3 * 0.01)) without (sloth_window)
          and
          max(slo:sli_error:ratio_rate1d{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"} > (3 * 0.01)) without (sloth_window)
      )
      or
      (
          max(slo:sli_error:ratio_rate6h{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"} > (1 * 0.01)) without (sloth_window)
          and
          max(slo:sli_error:ratio_rate3d{sloth_id="identity-sidecar-latency", sloth_service="identity-sidecar", sloth_slo="latency"} > (1 * 0.01)) without (sloth_window)
      )
    labels:
      plane: identity
      severity: ticket
      sloth_severity: ticket
    annotations:
      summary: Identity-sidecar latency error budget is burning
      title: (ticket) {{$labels.sloth_service}} {{$labels.sloth_slo}} SLO error budget
        burn rate is too fast.
